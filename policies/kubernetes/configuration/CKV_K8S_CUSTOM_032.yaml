---
metadata:
  id: "CKV_K8S_CUSTOM_032"
  name: "Ensure priority class is defined for production workloads"
  category: "KUBERNETES_CONFIGURATION"
  severity: "LOW"
  guideline: "Priority classes ensure critical workloads are scheduled first during resource contention, preventing eviction of important services in cloud environments."
definition:
  cond_type: "attribute"
  resource_types:
    - "Deployment"
    - "StatefulSet"
    - "DaemonSet"
  attribute: "spec.template.spec.priorityClassName"
  operator: "exists"
