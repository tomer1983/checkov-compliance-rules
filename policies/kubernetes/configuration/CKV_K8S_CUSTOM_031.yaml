---
metadata:
  id: "CKV_K8S_CUSTOM_031"
  name: "Ensure topology spread constraints are configured for optimal pod distribution"
  category: "KUBERNETES_CONFIGURATION"
  severity: "LOW"
  guideline: "Topology spread constraints enable fine-grained control over pod distribution across failure domains (zones, nodes, etc.) for better availability and resource utilization in cloud environments."
definition:
  cond_type: "attribute"
  resource_types:
    - "Deployment"
    - "StatefulSet"
    - "ReplicaSet"
  attribute: "spec.template.spec.topologySpreadConstraints"
  operator: "exists"
